{
  "name": "ipo-pilot-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-assisted DRHP drafting for SME IPOs — merchant banker and promoter workspace",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server start\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1101.0",
    "@aws-sdk/client-s3": "^3.1101.0",
    "@aws-sdk/lib-dynamodb": "^3.1101.0",
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "docx": "^8.5.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "multer-s3": "^3.0.1",
    "node-cron": "^4.6.0",
    "node-fetch": "^3.3.2",
    "pdfkit": "^0.19.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
